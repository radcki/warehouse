<Window x:Class="Wahehouse.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wahehouse.Gui"
        xmlns:valueConverters="clr-namespace:Wahehouse.Gui.ValueConverters"
        DataContext="{Binding Source={StaticResource Locator}, Path=MainViewModel}"
        mc:Ignorable="d"
        Title="Magazyn" Height="450" Width="800">
	<Window.Resources>
		<valueConverters:ImageSourceValueConverter x:Key="ImageSourceValueConverter" />
	</Window.Resources>
	<Grid>
        <Grid.RowDefinitions>
			<RowDefinition></RowDefinition>
			<RowDefinition Height="Auto"></RowDefinition>
		</Grid.RowDefinitions>

        <Grid Grid.Row="0">
			<Image Source="{Binding Path=PreviewImage, Converter={StaticResource ImageSourceValueConverter}, Mode=OneWay}"></Image>
		</Grid>
		<Grid Grid.Row="1">
			<ProgressBar Minimum="0" Maximum="10000" Height="30" Value="{Binding ProgressBarValue}"></ProgressBar>
		</Grid>
    </Grid>
</Window>
